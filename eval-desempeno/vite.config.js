import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// IMPORTANTE: Cambia este valor por el nombre exacto de tu repositorio en GitHub
// Ejemplo: si tu repo es github.com/andrea-vargas/eval-desempeno → pon '/eval-desempeno/'
// Si tu repo se llama igual que tu usuario (ej: andrea-vargas.github.io) → pon '/'
const REPO_NAME = '/tu-primera-evaluacion/'

export default defineConfig({
  plugins: [react()],
  base:andreavargaspsic-ai/Primeraevaluacion.git
})
